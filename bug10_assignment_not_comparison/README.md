**Error:** The script fails with a `SyntaxError: cannot assign to operator` because it uses a single equals sign `=` (assignment) instead of a double equals sign `==` (comparison) inside an `if` statement.

**Hint:** Assignment instead of comparison.

## Fixes
- **C**: `// FIX: Changed assignment operator '=' to comparison operator '==' in the if statement.`
- **C++**: `// FIX: Changed assignment operator '=' to comparison operator '==' in the if statement.`
- **Java**: `// FIX: Changed assignment operator '=' to comparison operator '==' in the if statement.`
- **JavaScript**: `// FIX: Changed assignment operator '=' to comparison operator '==' in the if statement.`
- **Python**: `// FIX: Changed assignment operator '=' to comparison operator '==' in the if statement.`
