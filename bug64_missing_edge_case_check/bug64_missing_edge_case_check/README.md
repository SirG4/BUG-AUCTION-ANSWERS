**Error:** The script fails due to an error in the implementation.

**Hint:** Missing edge case check.
Bug Type: Empty pairwise result caused error
Fixes Made:

If input size < 2 → return 0 instead of error

Terminal Output (All Languages):

0x536a532c7ec1641f