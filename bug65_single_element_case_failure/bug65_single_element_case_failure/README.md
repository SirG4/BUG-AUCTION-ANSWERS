**Error:** Pairwise_xor returned empty for single-element input {data}.

**Hint:** Single element case failure.
Bug Type: Division by zero when array empty
Fixes Made:

If array empty → set avg = 0 instead of dividing

Terminal Output (All Languages):

0x0